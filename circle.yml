machine:
  node:
    version: 7.5.0
  environment:
    NODE_ENV: test

dependencies:
  pre:
    - ./scripts/setupNpm.sh

deployment:
  all:
    branch: /.*/
    commands:
      - ./scripts/release.sh
