machine:
  node:
    version: 7.5.0
  environment:
    NODE_ENV: test

dependencies:
  pre:
    - ./scripts/setupNpm.sh

deployment:
  feature:
    branch: master
    commands:
      - ./scripts/npmDeploy.sh
  production:
    branch: develop
    commands:
      - ./scripts/npmDeploy.sh beta
